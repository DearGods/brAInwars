export const PlayerActionMsgEnum = {
	GAME_ENDED: "GAME ENDED" as string,
	GAME_STARTED: "GAME STARTED" as string,
	PLAYER_JOINED: "PLAYER JOINED" as string,
	PLAYER_LEFT: "PLAYER LEFT" as string,
} as Record<string, string>

export const PlayerActionMsgValues = [
	'GAME_ENDED',
	'GAME_STARTED',
	'PLAYER_JOINED',
	'PLAYER_LEFT'
];
